package com.knownassurajit.dvide_finance.app.ui.theme

import androidx.compose.ui.unit.dp

/**
 * Compact-window spacing. One gutter so chrome, heroes, and lists share an edge.
 *
 * IconButtons are 48dp with a 24dp glyph. Horizontal bar inset of 8dp puts that
 * glyph on the 20dp screen margin — the same line as body content.
 */
object DvideDimens {
    val screen = 20.dp
    val screenTight = 16.dp
    val barInset = 8.dp
    val section = 24.dp
    val item = 12.dp
    val tight = 8.dp
    val hairline = 4.dp
    val card = 20.dp
    val list = 16.dp
    val fabClearance = 96.dp
    val touch = 48.dp
    val commit = 56.dp
}
